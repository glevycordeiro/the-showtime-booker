<div class="container">
  <div class="row">
      <% @movies.each do |movie| %>
        <div class="col-12 col-sm-4">
          <%= render "card_movie", movie: movie %>
        </div>
      <% end %>
  </div>
</div>


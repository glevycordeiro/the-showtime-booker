<%= link_to movie_path(movie) do %>
<div class="card-trip">
  <%= image_tag movie.poster %>
  <div class="card-trip-infos">
    <div>
      <h2><%= movie.title %></h2>
    </div>
  </div>
</div>
<% end %>
